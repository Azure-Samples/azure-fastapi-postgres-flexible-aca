# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-fastapi-postgres-flexible-aca
metadata:
    template: azure-fastapi-postgres-flexible-aca
infra:
    provider: "bicep"
services:
    web:
        project: demo_code
        module: web
        language: py
        host: containerapp

